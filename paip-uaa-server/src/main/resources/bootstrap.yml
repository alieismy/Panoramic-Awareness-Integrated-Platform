server:
  port: 8000
  servlet:
    session:
      cookie:
        name: OAUTH2SESSION

spring:
  application:
    name: uaa-server

encrypt:
  key-store:
    location: classpath:paip.p12
    secret: Abcd123456
    alias: paip